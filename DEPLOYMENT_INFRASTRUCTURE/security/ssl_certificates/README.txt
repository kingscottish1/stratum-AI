Place TLS certificates / private keys here during local development ONLY.

NEVER commit real certificates or keys. In production use:
  - cert-manager + Let's Encrypt (Kubernetes)
  - AWS Certificate Manager (Terraform-managed)

This directory is git-ignored (see root .gitignore).
