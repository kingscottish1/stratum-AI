# TEMPLATE ONLY — never commit real values.
# Populate via: kubectl create secret generic stratum-secrets \
#   --from-env-file=secrets.env -n stratum
# Or better: external-secrets / SOPS + sealed-secrets (see security/secrets_management.md)
apiVersion: v1
kind: Secret
metadata:
  name: stratum-secrets
  namespace: stratum
type: Opaque
stringData:
  # Replace with real values through your secret manager, never inline.
  OPENAI_API_KEY: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME"
  TWILIO_ACCOUNT_SID: "CHANGE_ME"
  TWILIO_AUTH_TOKEN: "CHANGE_ME"
  HUBSPOT_API_KEY: "CHANGE_ME"
